export interface LoanSimulationDTO {
    amount: number;         // Jumlah pinjaman
    tenor: number;          // Tenor pinjaman
    plafondName: string;    // Nama plafon yang dipilih (contoh: 'Bronze', 'Silver', dll)
}
  
